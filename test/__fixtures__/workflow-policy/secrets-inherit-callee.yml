# Callee for the `secrets: inherit` fixture. Clean on purpose: the finding belongs to the CALLER
# job that passes the secrets, and this file must stay silent so the test cannot pass by accident.
name: secrets inherit callee
on:
  workflow_call:
permissions:
  contents: read
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo build
