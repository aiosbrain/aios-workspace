# VIOLATES pr-target-artifact-download — attacker-authored artifact unpacked in a privileged job.
name: violating prt artifact
on:
  pull_request_target:
permissions:
  contents: read
jobs:
  consume:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: pr-build
      - run: echo consumed
