# Routing fixtures for the aios-workspace toolkit resolver.
# Schema: ~/Projects/.tessera/resolver/fixtures-schema.yaml
# Run: node ~/Projects/.tessera/resolver/bin/routing-eval.mjs <this-repo>
fixtures:
  - intent: "Review this implementation plan before we build"
    expected: "review-plan"
  - intent: "Review this diff"
    expected: "code-review"
  - intent: "The agent says CI is green and tests were added — check it"
    expected: "ai-code-review"
  - intent: "Is AIO-75 actually done in code?"
    expected: "spec-status-reconciler"
  - intent: "Demo is tomorrow, will the build run?"
    expected: "demo-preflight-buildcheck"
  - intent: "Is this test suite actually wired into CI?"
    expected: "test-ci-wiring-audit"
  - intent: "Audit the decision log before close-out"
    expected: "decision-audit"
  - intent: "Are the deliverables drifting past the scope baseline?"
    expected: "scope-creep"
  - intent: "What happened this week?"
    expected: "weekly-synthesis"
  - intent: "Change how the scaffold stamps workspaces"
    expected: "Edit the template"
  - intent: "Add a new item kind to the sync protocol"
    expected: "brain-api.md"
  - intent: "Starting a feature that will commit"
    expected: "aios worktree add"
  - intent: "Ship this Linear issue"
    expected: "SPEC_READY"
