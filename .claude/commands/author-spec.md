---
description: Draft one cold-start buildable candidate spec without evaluating or publishing it
argument-hint: <source> [--repo <path>]
---

Use `author-ready-spec` to turn $ARGUMENTS into one bounded candidate spec.

Inspect the pinned repository baseline before making architecture claims. Preserve fixed decisions,
label new paths, make acceptance observable, and recommend no more than two compatible builder
skills from `.claude/skill-suite.json`. Write only a local candidate; do not evaluate, implement, or
mutate Linear.
