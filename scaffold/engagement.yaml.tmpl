# Engagement metadata — single source of truth for this team-ops repo.
slug: {{SLUG}}
client:
  name: "{{CLIENT_NAME}}"
  legal_name: "{{CLIENT_FULL_NAME}}"
description: "{{ENGAGEMENT_DESC}}"
started: "{{START_DATE}}"
currency: "{{CURRENCY}}"

captain: {{CAPTAIN}}
members:
{{MEMBERS_YAML}}

# Access tiers used across the spine: admin | team | client
# Sprint cadence, billing terms, and integrations are engagement-specific —
# add them here as the engagement defines them.
