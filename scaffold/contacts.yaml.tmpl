# Verified contact registry for {{CLIENT_NAME}}.
# Add entries only with confirmed details. Mark what each contact is safe to
# receive (e.g. deliverables, status updates) to support the publishing rules.
#
# Example:
# - name: "Full Name"
#   role: "Title"
#   side: client            # client | team
#   email: ""               # confirmed address only
#   safe_for: [status-updates]
#   notes: ""

contacts:
{{CONTACTS_YAML}}
