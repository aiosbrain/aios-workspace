---
access: admin
type: memory
updated: 2026-06-17
---
# Workspace memory — private (never synced)
<!-- Cap ~2,000 chars (this file is injected into the agent at session start).
     AGENT: when over cap, remove the least-useful line before adding a new one.
     HUMAN editors: same principle — prune, don't expand past the cap.
     Plain facts only — no instructions, no code blocks.
     Canonical, shareable company/role facts live in 0-context/ — this file only
     holds a short summary + the agent's private working knowledge (env, tooling). -->

- Company (1-line; details → `0-context/`):
- Environment / conventions:
- Tooling:
