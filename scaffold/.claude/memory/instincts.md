---
access: team
---
# Instincts — distilled rules

One line per rule. Every rule links the incidents it was distilled from
(`derived-from: incidents/<file>`). Consult this BEFORE running any harness;
add to it only via the progression in `README.md` (≥2 verified incidents).

<!--
Format (a real rule line starts with a dash; this example deliberately doesn't,
so OGR05 ignores it):
  **R1** <the rule, one sentence> — derived-from: incidents/<file-a>.md, incidents/<file-b>.md
-->

*(No rules yet — this repo hasn't failed enough to learn anything. Incidents
accumulate in `incidents/`; rules appear here when a pattern repeats.)*
