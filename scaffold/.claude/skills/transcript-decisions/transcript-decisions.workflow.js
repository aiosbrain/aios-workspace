/**
 * RETIRED — intentionally non-executable.
 *
 * AIO-370 retired the Workflow-tool transcript template. It used obsolete
 * `03-status` destinations, a `client` audience, decisions-only extraction, and a
 * Markdown-only return value that bypassed the typed review/apply state machine.
 *
 * The sole canonical behavior is now the `aios transcripts` CLI backed by the typed
 * meetings review engine. See SKILL.md for draft, list, approve, and approved-stage
 * push-retry guidance.
 *
 * This managed path remains as a notice for existing stamped workspaces. It exports
 * no metadata or workflow body and must not be invoked, copied, or adapted.
 */
