---
description: Pull today's Granola meeting transcripts and produce a daily digest.
status: active
---

Pull today's Granola meeting transcripts and produce a daily digest.

**Arguments:** $ARGUMENTS (optional date `YYYY-MM-DD`)

Use the `granola-digest` skill when Granola is connected. Save transcripts to
`1-inbox/transcripts/` and the consolidated digest to `2-work/` with `access: team`.

If Granola is not configured, explain how to connect it via `aios onboard` or
`.claude/INTEGRATIONS.md`.
