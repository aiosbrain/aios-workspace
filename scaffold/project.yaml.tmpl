# Project metadata — single source of truth for this team-ops repo.
okf_version: "0.1"
slug: {{SLUG}}
stakeholder:
  name: "{{CLIENT_NAME}}"
  legal_name: "{{CLIENT_FULL_NAME}}"
description: "{{ENGAGEMENT_DESC}}"
started: "{{START_DATE}}"
currency: "{{CURRENCY}}"

lead: {{CAPTAIN}}
members:
{{MEMBERS_YAML}}

# Access tiers used across the spine: admin | team | external
# (`client` is accepted as a legacy alias of `external`.)
# Sprint cadence and integrations are project-specific — add them here as
# the project defines them.
